package twisk.exceptionstwiskIG;

public class ExceptionArcIG extends TwiskException{

    /**
     * Constructeur ExceptionArcIG qui est appellé pour toutes les
     * exceptions qui peuvent être lévés, concernent les ArcIGs
     * @param message le message de l'exception
     */
    public ExceptionArcIG(String message){
        super(message);
    }
}
